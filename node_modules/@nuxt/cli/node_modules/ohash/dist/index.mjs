import { s as serialize } from './shared/ohash.CL_iaQBA.mjs';
import { digest } from 'ohash/crypto';
export { digest } from 'ohash/crypto';

function hash(input) {
  return digest(serialize(input));
}

export { hash, serialize };
